const initialState = {
    data: {

    },
    isFetching: false,
    isFetched: false,
    msg: '',
    error: false
}



export default (state = initialState, action) => {

    switch (action.type) {
        case '':
            
            break;
        default:
            return state;
    }
}